# Schemas and volumes are created by the setup job (notebooks/setup_infrastructure.py)
# rather than DABs resources, because DABs dev mode adds a per-user prefix to schema
# names that conflicts with the pipeline target and volume paths.
#
# The setup job creates:
#   - ${var.catalog}.${var.raw_schema} schema + incoming volume
#   - ${var.catalog}.${var.wiki_schema} schema + obsidian volume
